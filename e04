%{
    int i;
%}
l   [a-zA-Z]
d   [0-9]
%%
{l} ({l}|{d})*  {
                    for(i = 0; i < yyleng; i++){
                        yytext[i] = toupper(yytext[i]);
                    }
                    printf("%s", yytext);
}
.   ;
'\n'    ;