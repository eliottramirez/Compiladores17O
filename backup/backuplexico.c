/*int main (int argc, char *argv[]){
    yyin = fopen(argv[1], "r");
    
    idToken = yylex();
    while(idToken != 0){
        printf("%d: %s\n", idToken, yytext);
       
        idToken = yylex();
    }

    printf("\nNúmero de lineas: %d\n", numLinea);
}*/